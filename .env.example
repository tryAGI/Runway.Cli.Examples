# Copy to `.env` and fill in:
#   cp .env.example .env

# Required: your Runway API key (https://dev.runwayml.com/)
RUNWAY_API_KEY=

# Optional: pin the model used by `claude -p` for these examples.
# Cheaper than opus for routine work; override per run if you want better quality.
# CLAUDE_MODEL=sonnet
