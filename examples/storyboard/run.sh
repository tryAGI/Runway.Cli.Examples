#!/usr/bin/env bash
# storyboard: minimal user idea -> single expanded storyboard PNG.

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
NAME="storyboard"
BUDGET=2
. "$HERE/../../scripts/_runner.sh"
